version: '2'

services:
    web:
        build: .
        ports:
         - "8080:8080"
        links:
         - db
         - broker
    broker:
        image: rabbitmq
    db:
        image: postgres
        environment:
            POSTGRES_PASSWORD: apptrack
            POSTGRES_USER: apptrack
